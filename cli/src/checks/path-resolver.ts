/**
 * Re-export of the shared runtime path resolution utility.
 * Resolves a relative path against the config file directory,
 * workspace root, or current working directory.
 */
export { resolveRuntimeLikePath } from "../utils/path-resolver.js";
